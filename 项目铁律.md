# dev-task-scheduler 项目铁律

> **铁律锚点**：授权 → 备份 → 留痕
> **全局铁律卡引用**：`/Volumes/BR256G/DevProjectTeamSkill/.trae/skills/dev-project-team-skill/references/iron_rules.md`
> **生成时间**：2026-08-31
> **项目编号**：PRJ-TS-001
> **项目名称**：dev-task-scheduler

---

## 1. 本项目目录访问边界

| 边界类型 | 路径范围 | 读 | 写 | 删除 | 授权要求 | 有效期 | 状态 |
|---------|---------|---|---|-----|---------|--------|------|
| 项目根目录 | `/Volumes/BR256G/dev-task-scheduler/` | ✅ | ✅ | ✅ | 无需额外授权 | 长期 | 生效 |
| 项目外目录 | 其他项目目录、系统文件、仓库外路径 | ❌ | ❌ | ❌ | 需经 `register_auth` 授权（台账/14_授权登记.csv），未填有效期默认仅本次对话有效 | 会话级 | 禁用 |

> **铁律声明**：本项目可读写/删除范围 = **本项目所在目录（含子目录）**。项目目录之外的任何访问一律经授权登记，未授权禁止读写/删除。详见全局铁律卡 `iron_rules.md` §1。

---

## 2. 语言铁律（项目级硬约束 §8）

**本项目全程中文交流**（对话/文档/台账/报告/注释说明一律中文，代码标识符可保留英文）。

- 违反即阻断：任何非中文输出（除代码标识符外）视为违规，必须立即修正并留痕审计。

---

## 3. 产物落盘铁律（项目级硬约束 §9）

**所有生成产物必须落在本项目目录内**，禁止写入系统盘 / C 盘等非项目路径。

- 确需新建目录只能建在项目目录内且经用户确认。
- 违反即阻断：任何产物落盘至项目目录外视为违规，必须立即回收并留痕审计。

---

## 4. 继承的全局铁律卡（必须遵守）

| 编号 | 铁律名称 | 核心要求 |
|-----|---------|---------|
| §0 | 授权铁律 | 高危操作需：授权 + 备份 + 留痕 三条件齐备 |
| §1 | 目录访问边界 | 项目外访问需授权，未填有效期默认仅本次对话 |
| §2 | 备份铁律 | 修改前必须备份，备份留痕审计台账 |
| §3 | 留痕铁律 | 所有决策/操作必须留痕（台账/审计/交接） |
| §4 | 事实>文档 | 运行态与文档冲突时，以运行态为准并登记偏差 |
| §5 | 废弃清理门禁 | ADR 废弃后必须清理残留资产，未清完不得固化 |
| §6 | 三方一致性 | 需求-架构-代码追溯矩阵单一事实来源，断链驳回流转 |
| §7 | 大批量成本预警 | 超阈值须三选一确认（只做方案/分步执行/指定模型） |
| §8 | 执行合同闸门 | 副作用操作前走六段合同：理解→拆解→清单→确认→执行→复验 |
| §9 | 顺带操作禁令 | 未显式指令的操作禁止顺带执行 |

---

## 5. 违反处理机制

| 违反类型 | 处理动作 | 审计记录 |
|---------|---------|---------|
| 语言非中文 | 立即修正，重写为中文 | 追加 `台账/13_安全审计台账.csv` |
| 产物落盘越界 | 立即回收文件，恢复原状 | 追加 `台账/13_安全审计台账.csv` + `change_audit` |
| 未授权访问项目外 | 立即停止，补授权或回滚 | 追加 `台账/14_授权登记.csv` + `security_audit` |
| 绕过执行合同 | 回滚操作，重走六段合同 | 追加 `台账/13_安全审计台账.csv` |
| 顺带操作 | 撤销顺带动作，重新确认清单 | 追加 `台账/13_安全审计台账.csv` |

---

## 6. 铁律生效声明

本《项目铁律》自生成之刻起生效，贯穿项目全生命周期。任何阶段、任何角色、任何模型执行本项目任务时，**必须先读本文件**，以铁律锚点 `授权→备份→留痕` 为上下文锚点。

**未读本文件、未落实铁律锚点，禁止执行任何本项目任务。**

---

*文档版本：v1.0*  
*生成依据：DevProjectTeamSkill role-project-init v21.2.0 环节 1.5 write_project_iron_rules*  
*知识产权所有：段波（验证邮箱：duanbo.douglas@163.com）*